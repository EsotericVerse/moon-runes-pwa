__pycache__/
*.py[cod]
.pytest_cache/

# Private runtime corpora (never commit personal archives)
data/private/

# Local Cloudflare deployment config. Keep namespace ids / local deployment settings out of git.
cloudflare/wrangler.toml
