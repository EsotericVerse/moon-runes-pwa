name: Fix rune card format once

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/fix-rune-card-format-once.yml"

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python tools/fix_rune_card_format_once.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/rune.js css/style.css
          git diff --cached --quiet && exit 0
          git commit -m "fix: finalize unified rune card format"
          git push
