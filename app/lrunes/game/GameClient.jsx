'use client';
import {useEffect,useMemo,useState} from 'react';import{createGame,currentRound,evaluateDualCard,finalizeOpeningHand,normalizeRune}from'./game-engine';import{MACRO_STATES}from'./game-data';import'./game.css';
const labels={perfect:'完美 +2',pass:'過關 +1',fair:'尚可 +0',fail:'不行 −1'};
export default function GameClient(){const[runes,setRunes]=useState([]),[game,setGame]=useState(null),[selected,setSelected]=useState([[],[]]),[message,setMessage]=useState('載入月之符文…');useEffect(()=>{fetch('/data/json/core/runes.json').then(r=>r.json()).then(rows=>{const n=rows.map(normalizeRune);setRunes(n);setGame(createGame(n));setMessage('雙方先從 8 張起手牌各棄 3 張。');}).catch(()=>setMessage('無法載入符文資料。'));},[]);const round=useMemo(()=>game?currentRound(game):null,[game]);if(!game)return <main className="game-shell"><p>{message}</p></main>;const toggle=(pi,id)=>setSelected(prev=>{const next=prev.map(x=>[...x]),list=next[pi];if(list.includes(id))next[pi]=list.filter(x=>x!==id);else if(list.length<(game.stage==='setup'?3:2))list.push(id);return next;});const setup=()=>{try{setGame({...game,players:game.players.map((p,i)=>finalizeOpeningHand(p,selected[i])),stage:'play'});setSelected([[],[]]);setMessage('起手完成。R1 Event Phase。');}catch(e){setMessage(e.message);}};const test=()=>{const event=game.eventDeck[game.eventIndex];try{const o=game.players.map((p,i)=>evaluateDualCard(p.hand.filter(c=>selected[i].includes(c.id)),event));setMessage(`Alpha Match：A ${labels[o[0].result]}｜B ${labels[o[1].result]}`);}catch(e){setMessage(e.message);}};const event=game.eventDeck[game.eventIndex];return <main className="game-shell"><header><p className="eyebrow">LunaRunes × Game</p><h1>Semantic Playground</h1><p>Historical LOC2 → Current LunaRunes Game。</p><nav><a href="/lrunes/game/howto">玩法說明</a></nav></header><section className="status"><strong>{game.stage==='setup'?'起手設定':`R${round?.round} · ${round?.label}`}</strong><span>{message}</span></section>{game.stage==='play'&&round?.type==='event'&&<section className="event"><small>{event.id} · {event.group}</small><h2>{event.title}</h2><p>{event.description}</p><code>{event.signature}</code></section>}<section className="macro">{Object.entries(MACRO_STATES).map(([k,v])=><span key={k}><b>{k}</b> {v.zh}</span>)}</section><div className="players">{game.players.map((p,pi)=><section className="player" key={p.name}><h2>{p.name}</h2><p>De {p.de}/16 {p.star?'★':''}</p><div className="hand">{p.hand.map(c=><button type="button" className={selected[pi].includes(c.id)?'card selected':'card'} onClick={()=>toggle(pi,c.id)} key={c.id}><small>{String(c.id).padStart(2,'0')} · {c.group} · {c.macro||'SYSTEM'}</small><strong>{c.name}</strong><span>{c.action}</span></button>)}</div></section>)}</div><footer>{game.stage==='setup'?<button className="primary" onClick={setup}>雙方棄 3 張，開始</button>:round?.type==='event'?<button className="primary" onClick={test}>雙卡測試判定</button>:<p>Resonance Phase 將由 LOC2 v1.3 規則移植。</p>}</footer></main>}
