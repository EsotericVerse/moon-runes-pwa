// LunaRunes homepage copy intentionally stays Scope-local.
// Explanatory/reference material belongs to its own function/document pages, not the homepage.
export const RUNES_HOME_CONTENT = Object.freeze({});
