import ContextView from '../../loc/views/ContextView';

export const metadata = { title: '符文語意圖｜月之符文｜LOC' };

export default function RuneContextPage() {
  return <main className="loc-next-main" data-loc-view="context">
    <ContextView />
  </main>;
}
