import RuneDrawClient from '../RuneDrawClient';

export const metadata = { title: '抽牌｜月之符文', description: '月之符文抽牌。' };

export default function RuneDrawPage(){
  return <main className="loc-next-main"><RuneDrawClient /></main>;
}
