export default function RuneSemanticStatisticsPanel({summaries=[],comparison=[]}){return <section className="loc-card"><p className="loc-eyebrow">Rune Semantic Statistics</p><h2>ERA 符文語意差異</h2><p>此區只接受 Rune Algorithm 已確認的 semantic hits；不以 runes.json 關鍵詞清單的重複次數冒充 corpus 符文統計。</p>{!summaries.length?<p className="loc-status">尚未提供已重新分類的 corpus 統計資料。Rune Algorithm 模組已可供 corpus builder 重掃。</p>:<><div className="loc-context-list">{summaries.map(summary=><article className="loc-context-item compact" key={summary.era}><b>{summary.era}</b><span>{summary.documents} 筆文件</span><small>{summary.runes.slice(0,12).map(x=>`${x.rune} ${x.count}`).join(' · ')}</small></article>)}</div>{comparison.length>0&&<div className="loc-ranking">{comparison.slice(0,16).map(row=><div key={row.rune}><b>{row.rune}</b><span>{row.old} → {row.recent} · Δ {row.delta>0?'+':''}{row.delta}</span></div>)}</div>}</>}</section>}
