import {CULTURE_CHANGE_MODES,LUNARUNES_ERAS,LUNARUNES_ERA_BOUNDARIES} from '../model/rune-era';
export default function RuneEraPanel(){return <section className="loc-card"><p className="loc-eyebrow">LunaRunes ERA</p><h2>符文正式時期</h2><p>正式邊界：<strong>{LUNARUNES_ERA_BOUNDARIES}</strong>。40 與 64 僅是 RC 過渡狀態，不另立正式 ERA。</p><div className="loc-context-list">{LUNARUNES_ERAS.map(era=><article className="loc-context-item compact" key={era.id}><div className="loc-result-meta"><span>{era.label}</span><span>{era.runeCount} 符</span></div><b>{era.description}</b>{era.rcCount&&<small>RC：{era.rcCount}</small>}</article>)}</div><p className="loc-status">文化觀察模式：{CULTURE_CHANGE_MODES.join('、')}。只描述已發生的歷史與語意變化，不預測未來。</p></section>}
