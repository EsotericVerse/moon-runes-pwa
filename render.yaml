services:
  - type: web
    name: fortune-api
    runtime: python
    rootDir: card_api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
