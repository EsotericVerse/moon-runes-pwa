services:
  - type: web
    name: fortune-api
    runtime: python
    rootDir: card_api
    buildCommand: pip install -r requirements.txt && python build_facebook_keyword_index.py
    startCommand: uvicorn runtime_app:app --host 0.0.0.0 --port $PORT
