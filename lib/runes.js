// Migration adapter: one canonical rune module, no JSON runtime parsing.
// During phase 2 the canonical implementation can move here without changing consumers.
export { rune, groups } from '../js/runes.js';
