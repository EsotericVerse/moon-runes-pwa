# Legacy NAV compatibility

`js/loc-nav.js` exists only for legacy static HTML compatibility. Its Current behavior follows the same Scope NAV contract as `app/ScopeNav.jsx`.

The authoritative Current terminology is **NAV**. Page-local menus are local function menus, not additional NAV layers.
