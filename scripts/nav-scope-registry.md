# Scope registration

A Scope registers: host/base path, governed reserved first NAV entry, optional role links, and home links. Shared NAV functions are inherited. Adding a Scope must not require copying NAV markup into each page.
