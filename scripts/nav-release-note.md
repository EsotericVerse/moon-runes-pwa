# NAV release note

NAV now resolves shared functions from the active Scope instead of hard-coding LOC destinations. LunaRunes keeps equivalent `lrunes.lo3rwang.cc/*` and `loc.lo3rwang.cc/lrunes/*` navigation semantics. Current terminology is one NAV only, with local menus kept separate. Scope registration is centralized and CI-guarded.
