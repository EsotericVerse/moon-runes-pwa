# No per-page NAV duplication

Pages do not define their own full NAV. They render the centralized NAV and may define only page-local function menus. Scope routing belongs to the centralized resolver/registry.
