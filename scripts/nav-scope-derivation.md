# Scope derivation

The NAV does not enumerate separate copies of shared functions per page. It derives the current Scope from host/path, loads that Scope's reserved/role/home configuration, and applies the shared function set to the Scope base path.
