# NAV authority

Current authority chain for navigation:

`Scope Canon → docs/NAV_GOVERNANCE.md → declarative Scope route map → runtime Scope resolver → rendered NAV → CI guards`

Rendered links and legacy compatibility code cannot override the upstream Scope/NAV Canon.
